Removed Mermaid diagram content.

Update needed: the prompt file should request a step-by-step written interaction description (no Mermaid/code), showing how users interact with the React UI and which Flask API endpoints they call, including loading/empty/error/cancel/disabled states.

